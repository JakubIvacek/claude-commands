# Changelog

## Unreleased

- `/release` now creates a minimal `CHANGELOG.md` when documenting a change in a repo that has no backlog/todo file and no changelog yet, instead of silently skipping documentation.
